{{ form_start(form) }}
<div class="form-row">
    <div class="form-group col-md-6">
        {{ form_row(form.personne.firstName) }}
    </div>
    <div class="form-group col-md-6">
        {{ form_row(form.personne.lastName) }}
    </div>
    <div class="form-group col-md-6">
        {{ form_row(form.email) }}
    </div>
    <div class="form-group col-md-6">
        {{ form_row(form.address) }}
    </div>
    <div class="form-group col-md-6">
        {{ form_row(form.postal_code) }}
    </div>
    <div class="form-group col-md-6">
        {{ form_row(form.hbd_day) }}
    </div>
    <div class="form-group col-md-6">
        {{ form_row(form.phone_number) }}
    </div>
    <div class="form-group col-md-6">
        {{ form_row(form.city) }}
    </div>
</div>
<div class="btn-group">
    <a class="btn btn-dark" href="{{ path('admin_client_index') }}">back to list</a>
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
</div>
    {{ form_widget(form) }}
{{ form_end(form) }}
