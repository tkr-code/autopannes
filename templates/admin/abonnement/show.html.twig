{% extends 'base.html.twig' %}

{% block title %}Abonnement{% endblock %}

{% block body %}
    <h1>Abonnement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ abonnement.id }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ abonnement.createdAt ? abonnement.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>End_at</th>
                <td>{{ abonnement.endAt ? abonnement.endAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ abonnement.etat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_abonnement_index') }}">back to list</a>

    <a href="{{ path('admin_abonnement_edit', {'id': abonnement.id}) }}">edit</a>

    {{ include('admin_abonnement/_delete_form.html.twig') }}
{% endblock %}
